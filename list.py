#print 5 names
names = ['John', 'Paul', 'George', 'Ringo', 'Pete'] #list of names
for name in names:
    print(name) #print each name
# The list of names is stored in the variable names. The for loop iterates over each name in the list and prints it.    
